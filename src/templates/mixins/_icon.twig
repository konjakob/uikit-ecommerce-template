{% macro icon(name, ratio) %}

	{% if not ratio %}
		span(uk-icon= name)&attributes(attributes)
	{% else %}
		span(uk-icon=`icon: ${name}; ratio: ${ratio};`)&attributes(attributes)
	{% endif %}
	
{% endmacro %}